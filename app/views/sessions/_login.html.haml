-# Reusable login
%p
  .two-column-layout

    / Login form
    .tcl-column

      = render :partial => "sessions/login_form", :locals => {:title => title}

    / Help text
    .tcl-column

      = render :partial => 'sessions/login_sidebar', :locals => {:standalone => standalone}
