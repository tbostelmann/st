-@page_title= :community.l.titleize

.content

  %h1
    =:donor_community.l.titleize

  .single-column

    .pagination.upper
      = paginating_links @donors, :params => params if @donors.page_count > 1
  
    = render :partial => "donors/community", :locals => {:donors => @donors}
  
    .pagination.lower
      = paginating_links @donors, :params => params if @donors.page_count > 1
  