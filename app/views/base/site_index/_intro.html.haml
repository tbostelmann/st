.panel.banner
  .section
    =render :partial => "base/site_index/intro/arrow_graphic_placeholder_remove_me"

  .section.impact
    =render :partial => "base/site_index/success_stories/impact"

  .section
    -classes = ["one", "two", "three"]
    -::TRIPTYCH_PANELS.each_with_index do |panel, i|
      .panel.triptych
        =render :partial => "base/site_index/intro/triptych", :locals =>{ :panel => panel, :which => "#{classes[i]}"}

.panel.success-story
  =render :partial => "base/site_index/intro/featured_success_story", :locals => {:successful_saver => successful_saver}
